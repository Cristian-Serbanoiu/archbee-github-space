# Archbee Space

Documentation space for blocks. Content lives in `NOXUS/` and is structured in **archbee.json**.

**Categories:** **BASIC**, **MEDIA**, **DEVELOPER** (see `archbee.json` for order and paths).

Start with `NOXUS/BASIC/heading-expandable-blocks.md` or browse the sidebar in **archbee.json**.
